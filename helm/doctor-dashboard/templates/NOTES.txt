Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }} -n {{ include "doctor-dashboard.namespace" . }}
  $ helm get all {{ .Release.Name }} -n {{ include "doctor-dashboard.namespace" . }}

{{- if .Values.ingress.enabled }}

{{- range $host := .Values.ingress.hosts }}
Doctor Dashboard will be accessible at:
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}
{{- end }}

{{- else }}

The service is not exposed via ingress. To access the application, use port-forwarding:

  $ kubectl port-forward -n {{ include "doctor-dashboard.namespace" . }} svc/{{ include "doctor-dashboard.fullname" . }} 8001:8001

Then access at: http://localhost:8001
{{- end }}

Check the deployment status:

  $ kubectl get pods -n {{ include "doctor-dashboard.namespace" . }} -l app.kubernetes.io/name={{ .Chart.Name }}

Check application logs:

  $ kubectl logs -n {{ include "doctor-dashboard.namespace" . }} deployment/{{ include "doctor-dashboard.fullname" . }} -f

To upgrade the release:

  $ helm upgrade {{ .Release.Name }} . -n {{ include "doctor-dashboard.namespace" . }}

To rollback to a previous revision:

  $ helm rollback {{ .Release.Name }} -n {{ include "doctor-dashboard.namespace" . }}

To uninstall the release:

  $ helm uninstall {{ .Release.Name }} -n {{ include "doctor-dashboard.namespace" . }}
